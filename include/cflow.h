#include "finQED.h"

count_sign_changes(v); 	

struct cflow  {
    static const decl CFLOW_ERROR = .NaN;	
    const decl
        Nv,
        amounts,
        times;
    cflow(inflow,intimes=0);
    pv(r);
    irr();
    multiple_roots();
    single_irr();
    modified_irr(financerate, reinvestmentrate);
    unique_irr();
    breakeven();
    print_summary(finrate, investrate);
    }
